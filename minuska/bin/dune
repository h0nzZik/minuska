(ocamllex
  (modules lexer)
)

(menhir
  (modules parser)
  (flags -la 2)
)


(executable
 (public_name minuska)
 (name main)
 (modules syntax parser lexer main)
 (libraries core core_unix.command_unix)
 )
